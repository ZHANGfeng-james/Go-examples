

`Getter` ==接口包装==了 `Value` 接口：

~~~go
type Value interface {
    String() string
    Set(string) error
}

type Getter interface {
    Value  // Getter 接口包装了 Value 接口
    Get() interface{}
}
~~~

